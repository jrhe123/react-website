import styled, {css} from 'styled-components';

export const ChartDiv = styled.div`
  
`;

export const ChartFormDiv = styled.div`
  
  	border-radius: 6px;
	padding: 36px 24px;
	width: 1000px;
	margin: 0 auto;
	box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
	background: #fff;
`;